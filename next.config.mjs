/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
    reactStrictMode: true,
    env: {
      API_KEY: process.env.API_KEY,
    },
};
  

// export default nextConfig;
